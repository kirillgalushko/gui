# PhoneInput

`PhoneInput` formats Russian phone numbers while typing, keeps the cursor position and accepts only phone characters. Use it with `v-model`; validate the value with the exported `isValidPhone` helper in a form schema.
