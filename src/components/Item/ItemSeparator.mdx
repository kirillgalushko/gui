# ItemSeparator

`ItemSeparator` визуально разделяет соседние строки внутри `ItemGroup`.

```vue
<ItemGroup>
  <Item>Первая строка</Item>
  <ItemSeparator />
  <Item>Вторая строка</Item>
</ItemGroup>
```

Компонент имеет семантическую роль `separator` и не используется вне группы элементов.
